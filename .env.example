# Pulled from Neon by `npx neon@latest env pull`.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
NEON_AUTH_BASE_URL=

# Generate once and keep the same value in every Vercel environment.
# Example: openssl rand -base64 32
NEON_AUTH_COOKIE_SECRET=
